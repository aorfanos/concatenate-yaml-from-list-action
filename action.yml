name: "Simple concatenate YAML files action"
description: "Append YAML files to a single one using a prefix"
author: aorfanos
runs:
  using: "composite"
  steps:
    - run: python ${{ github.action_path }}/yaml-concat.py
      shell: bash
branding:
  color: green
  icon: cloud
